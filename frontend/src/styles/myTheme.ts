import { createTheme } from "@mui/material/styles";

export const myTheme: any = createTheme({
    palette: {
        primary: {
            main: '#fff'
        },
        secondary: {
            main: '#e91e63'
        }
    }
});
